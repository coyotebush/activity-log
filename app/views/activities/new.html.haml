%h1 New activity

= error_messages_for :activity

- form_for(:activity, :url => @activity) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag 'Create'

= link_to 'Back', activities_path
